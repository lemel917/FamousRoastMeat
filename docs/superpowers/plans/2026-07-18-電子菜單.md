# 名門燒臘電子菜單 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 兩頁式靜態網站——客人看的電子菜單頁 + 老闆切「賣完」的後台頁，狀態透過 Firebase 即時同步，部署在 GitHub Pages。

**Architecture:** 純靜態 HTML/CSS/JS（無打包工具、無 Node.js），菜單品項寫死在 `menu-data.js`；Firebase Realtime Database 只存 `soldOut/{品項編號}: true`；兩頁都用 Firebase SDK 的 `onValue` 即時監聽，後台切換後菜單頁一秒內自動變灰。

**Tech Stack:** HTML5 / CSS3 / 原生 JavaScript（ES Modules）、Firebase JS SDK v12.0.0（CDN 載入）、GitHub Pages、Google Fonts（Noto Serif TC）。

**Spec:** `docs/superpowers/specs/2026-07-18-電子菜單-design.md`

## Global Constraints

- 工作目錄（repo 根目錄）：`D:\AI\練習\名門燒臘電子菜單\FamousRoastMeat`，所有指令在此執行
- 所有檔案編碼 **UTF-8（無 BOM）**、`lang="zh-Hant"`、介面文字一律繁體中文
- **禁止**引入 Node.js、npm 套件、打包工具；Firebase SDK 一律用 CDN：`https://www.gstatic.com/firebasejs/12.0.0/firebase-app.js` 與 `.../firebase-database.js`
- Firebase 路徑固定為 `soldOut/{id}`，值一律 `true`；恢復供應＝刪除節點（絕不寫 `false`）
- 品項編號固定：便當 `b01`–`b19`、拼盤 `p01`–`p11`，順序照 PDF
- git 身分已設為 local config `lemel917 <lemel917@users.noreply.github.com>`，commit 不得使用公司信箱
- 本機測試伺服器：`python -m http.server 8000`（若無 Python 改用 `npx http-server -p 8000`），一律用 `http://localhost:8000` 開頁（ES Modules 不能用 `file://`）
- commit message 用繁體中文，結尾加 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

---

### Task 1: 專案骨架與菜單資料（menu-data.js）

**Files:**
- Create: `menu-data.js`
- Create: `css/base.css`

**Interfaces:**
- Produces: 全域常數 `MENU_DATA`，形狀為 `{ bento: {title, badge, columns:[string,string], items:[{id, name, price, priceExtra, tag?}]}, platter: {title, badge, items:[{id, name, price, note?, tag?}]} }`。後續所有任務依賴此形狀與品項編號。
- Produces: `css/base.css` 的 CSS 變數 `--bg --paper --line --brown --ink --red --gold` 與 `#sync-banner` 樣式，兩頁共用。

- [ ] **Step 1: 建立 `menu-data.js`**（內容照 PDF，共 30 項）

```js
// 菜單資料：要改菜單（品名/價格/標籤）就改這個檔案，改完 push 上 GitHub 即生效。
// id 是品項的固定編號，Firebase 的賣完狀態靠它對應，「不要」更動既有 id。
const MENU_DATA = {
  bento: {
    title: "便當",
    badge: "飯",
    columns: ["一般", "加肉"],
    items: [
      { id: "b01", name: "叉燒飯", price: 130, priceExtra: 190, tag: "人氣" },
      { id: "b02", name: "燒鴨飯", price: 135, priceExtra: 195 },
      { id: "b03", name: "叉鴨飯", price: 135, priceExtra: 195 },
      { id: "b04", name: "叉雞飯", price: 135, priceExtra: 195 },
      { id: "b05", name: "三寶飯", price: 135, priceExtra: 195, tag: "人氣" },
      { id: "b06", name: "三燒飯", price: 135, priceExtra: 195 },
      { id: "b07", name: "雞鴨飯", price: 135, priceExtra: 195 },
      { id: "b08", name: "燒肉飯", price: 135, priceExtra: 195 },
      { id: "b09", name: "燒肉叉燒飯", price: 135, priceExtra: 195 },
      { id: "b10", name: "燒肉雞飯", price: 135, priceExtra: 195 },
      { id: "b11", name: "燒肉鴨飯", price: 135, priceExtra: 195 },
      { id: "b12", name: "燒肉臘腸飯", price: 135, priceExtra: 195 },
      { id: "b13", name: "臘味飯", price: 135, priceExtra: 195 },
      { id: "b14", name: "臘腸鴨飯", price: 135, priceExtra: 195 },
      { id: "b15", name: "臘腸雞飯", price: 135, priceExtra: 195 },
      { id: "b16", name: "臘腸叉燒飯", price: 135, priceExtra: 195 },
      { id: "b17", name: "油雞腿飯", price: 140, priceExtra: 200 },
      { id: "b18", name: "招牌飯", price: 170, priceExtra: 230, tag: "招牌" },
      { id: "b19", name: "鴨腿飯", price: 170, priceExtra: 230 },
    ],
  },
  platter: {
    title: "燒臘拼盤",
    badge: "盤",
    items: [
      { id: "p01", name: "美味雙拼", price: 280, tag: "人氣" },
      { id: "p02", name: "美味三拼", price: 380, note: "三人份" },
      { id: "p03", name: "美味四拼", price: 480, note: "四人份" },
      { id: "p04", name: "燒鴨 1/4隻", price: 350, note: "三人份" },
      { id: "p05", name: "燒鴨 半隻", price: 700, note: "六人份" },
      { id: "p06", name: "燒鴨 一隻", price: 1400, note: "十二人份" },
      { id: "p07", name: "叉燒 一份", price: 350, note: "三人份" },
      { id: "p08", name: "燒肉 一份", price: 350, note: "三人份" },
      { id: "p09", name: "油雞腿 一隻", price: 110 },
      { id: "p10", name: "鴨腿 一隻", price: 140 },
      { id: "p11", name: "滷蛋 一顆", price: 15 },
    ],
  },
};
```

- [ ] **Step 2: 核對資料**

逐項對照 `D:\AI\練習\名門燒臘電子菜單\名門燒臘電子菜單_設計版.pdf`：便當 19 項（叉燒飯 130/190，油雞腿飯 140/200，招牌飯與鴨腿飯 170/230，其餘 135/195）、拼盤 11 項、人氣＝叉燒飯/三寶飯/美味雙拼、招牌＝招牌飯。
Expected: 品名、價格、標籤、備註與 PDF 完全一致。

- [ ] **Step 3: 建立 `css/base.css`**（兩頁共用底層）

```css
:root {
  --bg: #ece0c4;      /* 頁面米色底 */
  --paper: #f9f2df;   /* 菜單紙色 */
  --line: #d3c197;    /* 分隔線 */
  --brown: #8a6a3b;   /* 主棕色 */
  --ink: #4a3b22;     /* 內文深棕 */
  --red: #b02a1c;     /* 價格紅 */
  --gold: #a5772e;    /* 金色點綴 */
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  font-family: "Noto Serif TC", "PMingLiU", "Microsoft JhengHei", serif;
  background: var(--bg);
  color: var(--ink);
  min-height: 100vh;
  padding: 16px;
  -webkit-font-smoothing: antialiased;
}

button { cursor: pointer; font-family: inherit; }

/* 尚未收到 Firebase 第一筆資料前顯示的同步提示 */
#sync-banner {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 10;
  background: var(--brown);
  color: #fff;
  text-align: center;
  padding: 6px 12px;
  font-size: 14px;
  letter-spacing: 2px;
}
#sync-banner[hidden] { display: none; }
```

- [ ] **Step 4: Commit**

```bash
git add menu-data.js css/base.css
git commit -m "新增菜單資料與共用樣式底層

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: 菜單頁靜態版面（index.html，先不接 Firebase）

**Files:**
- Create: `index.html`
- Create: `css/menu.css`
- Create: `js/menu.js`

**Interfaces:**
- Consumes: 全域 `MENU_DATA`（Task 1）、`css/base.css` 的變數與 `#sync-banner`
- Produces: `js/menu.js` 匯出層級的兩個函式供 Task 4 使用——`renderMenu()`（初次渲染，本任務呼叫）與 `applySoldOut(soldOut)`（接受 `{[id]: true}` 物件，切換 `.sold-out` class）。每個品項元素為 `li.item[data-id]`。

- [ ] **Step 1: 建立 `index.html`**

```html
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>名門燒臘｜電子菜單</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+TC:wght@500;700;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/base.css">
  <link rel="stylesheet" href="css/menu.css">
</head>
<body>
  <div id="sync-banner">賣完狀態同步中，請稍候⋯</div>

  <div class="sheet">
    <header class="menu-header">
      <div class="brand">
        <h1 class="logo">名門燒臘</h1>
        <p class="logo-sub">MINGMEN SHAOLA</p>
      </div>
      <div class="header-right">
        <p class="slogan">招牌燒臘・精選便當</p>
        <p class="slogan-en">MING MEN SHAO LA</p>
      </div>
    </header>

    <main id="menu-root" class="menu-columns"></main>

    <footer class="menu-footer">現點現切・外帶歡迎</footer>
  </div>

  <script src="menu-data.js"></script>
  <script type="module" src="js/menu.js"></script>
</body>
</html>
```

- [ ] **Step 2: 建立 `js/menu.js`**（本任務只有渲染，Firebase 在 Task 4 加）

```js
// 菜單頁：先用本地 MENU_DATA 渲染（離線也看得到菜單），賣完狀態由 Firebase 即時套用（Task 4）。

function renderMenu() {
  const root = document.getElementById("menu-root");
  root.append(renderCategory(MENU_DATA.bento), renderCategory(MENU_DATA.platter));
}

function renderCategory(cat) {
  const section = document.createElement("section");
  section.className = "category";

  const head = document.createElement("div");
  head.className = "category-head";
  head.innerHTML =
    `<span class="category-badge">${cat.badge}</span>` +
    `<h2 class="category-title">${cat.title}</h2>`;
  section.append(head);

  // 便當在寬螢幕分成左右兩欄（各自帶「一般｜加肉」欄位標頭），拼盤單欄
  const groups = cat.columns
    ? [cat.items.slice(0, Math.ceil(cat.items.length / 2)), cat.items.slice(Math.ceil(cat.items.length / 2))]
    : [cat.items];

  const lists = document.createElement("div");
  lists.className = "item-lists";
  for (const group of groups) {
    const ul = document.createElement("ul");
    ul.className = "item-list";
    if (cat.columns) {
      const cols = document.createElement("li");
      cols.className = "price-cols";
      cols.innerHTML = `<span></span><span>${cat.columns[0]}</span><span>${cat.columns[1]}</span>`;
      ul.append(cols);
    }
    for (const item of group) ul.append(renderItem(item, Boolean(cat.columns)));
    lists.append(ul);
  }
  section.append(lists);
  return section;
}

function renderItem(item, hasTwoPrices) {
  const li = document.createElement("li");
  li.className = hasTwoPrices ? "item" : "item platter";
  li.dataset.id = item.id;

  const tag = item.tag
    ? `<span class="tag${item.tag === "招牌" ? " tag-signature" : ""}">${item.tag}</span>`
    : "";
  const note = item.note ? `<span class="note">（${item.note}）</span>` : "";
  const prices = hasTwoPrices
    ? `<span class="price">${item.price}</span><span class="price">${item.priceExtra}</span>`
    : `<span class="price">${item.price}</span>`;

  li.innerHTML =
    `<span class="item-name">${item.name}${tag}${note}</span>` +
    prices +
    `<span class="stamp">已賣完</span>`;
  return li;
}

// 依 Firebase soldOut 物件（如 {b01:true}）切換每列的 .sold-out
function applySoldOut(soldOut) {
  document.querySelectorAll(".item").forEach((el) => {
    el.classList.toggle("sold-out", soldOut[el.dataset.id] === true);
  });
}

renderMenu();

export { renderMenu, applySoldOut };
```

- [ ] **Step 3: 建立 `css/menu.css`**

```css
/* ===== 菜單紙外框（仿 PDF 雙線圓角框） ===== */
.sheet {
  max-width: 1280px;
  margin: 0 auto;
  background: var(--paper);
  border: 2px solid var(--brown);
  outline: 1px solid var(--brown);
  outline-offset: 4px;
  border-radius: 14px;
  padding: 32px 44px 24px;
}

/* ===== 頁首 ===== */
.menu-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid var(--line);
  padding-bottom: 20px;
}
.logo { font-size: 44px; font-weight: 900; letter-spacing: 6px; color: #2e2415; }
.logo-sub { font-size: 10px; letter-spacing: 4px; color: var(--gold); }
.slogan { font-size: 22px; font-weight: 700; letter-spacing: 8px; color: var(--brown); }
.slogan-en { font-size: 10px; letter-spacing: 5px; color: var(--gold); text-align: right; margin-top: 4px; }

/* ===== 兩大分類左右排 ===== */
.menu-columns {
  display: grid;
  grid-template-columns: 1.7fr 1fr;
  gap: 40px;
  padding-top: 24px;
}

.category-head { display: flex; align-items: center; gap: 10px; margin-bottom: 14px; }
.category-badge {
  border: 1px solid var(--brown);
  color: var(--brown);
  border-radius: 6px;
  padding: 2px 7px;
  font-size: 15px;
}
.category-title {
  background: linear-gradient(#8a6a3b, #6d5227);
  color: #f7edd2;
  border: 1px solid #503c1e;
  border-radius: 6px;
  padding: 5px 24px;
  font-size: 22px;
  letter-spacing: 14px;
  text-indent: 14px; /* 平衡 letter-spacing 造成的右偏 */
}

/* ===== 品項列 ===== */
.item-lists { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 0 44px; }
.item-list { list-style: none; }

.price-cols {
  display: grid;
  grid-template-columns: 1fr 60px 60px;
  font-size: 13px;
  color: #8c7a55;
  text-align: right;
  padding: 4px 0;
}

.item {
  position: relative;
  display: grid;
  grid-template-columns: 1fr 60px 60px;
  align-items: center;
  min-height: 52px;
  padding: 8px 0;
  border-bottom: 1px solid var(--line);
}
.item.platter { grid-template-columns: 1fr auto; }

.item-name { font-size: 18px; font-weight: 500; letter-spacing: 2px; }
.price {
  color: var(--red);
  font-weight: 700;
  font-size: 21px;
  text-align: right;
  font-family: Georgia, "Noto Serif TC", serif;
}
.tag {
  background: var(--red);
  color: #fff;
  font-size: 11px;
  border-radius: 999px;
  padding: 2px 8px;
  margin-left: 8px;
  vertical-align: 3px;
  letter-spacing: 1px;
}
.tag-signature { background: var(--gold); }
.note { font-size: 12px; color: #8c7a55; margin-left: 4px; }

/* ===== 賣完狀態 ===== */
.item .stamp { display: none; }
.item.sold-out .item-name,
.item.sold-out .note { color: #b5a988; }
.item.sold-out .tag { opacity: 0.4; }
.item.sold-out .price { color: #b5a988; text-decoration: line-through; }
.item.sold-out .stamp {
  display: block;
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%) rotate(-8deg);
  border: 2px solid var(--red);
  color: var(--red);
  background: rgba(249, 242, 223, 0.9);
  border-radius: 4px;
  padding: 1px 10px;
  font-size: 14px;
  font-weight: 700;
  letter-spacing: 3px;
}

.menu-footer {
  text-align: center;
  letter-spacing: 8px;
  color: #8c7a55;
  font-size: 14px;
  padding-top: 24px;
}

/* ===== RWD ===== */
@media (max-width: 960px) {
  .menu-columns { grid-template-columns: 1fr; gap: 28px; }
  .sheet { padding: 24px 20px; }
  .menu-header { flex-direction: column; gap: 10px; text-align: center; }
  .slogan-en { text-align: center; }
}
@media (max-width: 600px) {
  body { padding: 10px; }
  .logo { font-size: 34px; }
  .slogan { font-size: 18px; letter-spacing: 5px; }
  .item { grid-template-columns: 1fr 52px 52px; min-height: 56px; }
  .item-name { font-size: 17px; letter-spacing: 1px; }
  .price { font-size: 19px; }
}
```

- [ ] **Step 4: 本機驗證版面**

```bash
python -m http.server 8000
```

用瀏覽器開 `http://localhost:8000/`，檢查：
1. 米色紙面、雙線圓角外框、左上「名門燒臘」右上「招牌燒臘・精選便當」、底部「現點現切・外帶歡迎」
2. 便當區左右兩欄各有「一般｜加肉」標頭，共 19 項；拼盤區 11 項單價靠右
3. 叉燒飯/三寶飯/美味雙拼有紅色「人氣」、招牌飯有金色「招牌」；三人份等備註以小字顯示
4. 頂端有棕色「賣完狀態同步中」條（此階段還沒接 Firebase，常駐是正常的）
5. 視窗縮到 600px 寬：單欄、字級加大、價格不換行
6. DevTools Console 無紅字錯誤

Expected: 以上全部符合。

- [ ] **Step 5: Commit**

```bash
git add index.html css/menu.css js/menu.js
git commit -m "新增菜單頁靜態版面與渲染邏輯

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Firebase 專案建立與連線設定

**Files:**
- Create: `js/firebase-config.js`

**Interfaces:**
- Produces: 全域常數 `FIREBASE_CONFIG`（Firebase Web App 設定物件，含 `databaseURL`），Task 4、Task 5 的模組直接引用。
- Produces: 雲端 Realtime Database，規則為 `soldOut` 可讀寫、其餘拒絕。

- [ ] **Step 1: 引導使用者建立 Firebase 專案**（此步驟由使用者在瀏覽器操作，執行者提供以下指引並等待使用者貼回設定）

> 1. 開 https://console.firebase.google.com/ →「建立專案」→ 名稱如 `mingmen-menu`（Google Analytics 可關閉）
> 2. 左側「建構 → Realtime Database」→「建立資料庫」→ 地區選 **asia-southeast1（新加坡）** → 以「鎖定模式」啟動
> 3. 「規則」分頁貼上下方規則後按「發布」：
> ```json
> {
>   "rules": {
>     "soldOut": {
>       ".read": true,
>       ".write": true
>     }
>   }
> }
> ```
> 4. 專案總覽 → 齒輪「專案設定」→「你的應用程式」→ 新增「網頁應用程式」（暱稱如 `menu-web`，不勾 Hosting）→ 把畫面上 `const firebaseConfig = {...}` 的整段物件複製給我
> 5. 注意：物件裡必須有 `databaseURL` 欄位；若沒有，回到 Realtime Database 頁面上方複製資料庫網址（`https://xxx.firebasedatabase.app`）一併給我

- [ ] **Step 2: 建立 `js/firebase-config.js`**（把使用者提供的值填入；以下為結構示意，實際值以使用者提供為準）

```js
// Firebase Web App 設定。
// 注意：apiKey 不是密碼，公開在前端是 Firebase 的正常用法；真正的存取控制在資料庫規則。
const FIREBASE_CONFIG = {
  apiKey: "（使用者提供）",
  authDomain: "（使用者提供）.firebaseapp.com",
  databaseURL: "https://（使用者提供）.asia-southeast1.firebasedatabase.app",
  projectId: "（使用者提供）",
  storageBucket: "（使用者提供）.firebasestorage.app",
  messagingSenderId: "（使用者提供）",
  appId: "（使用者提供）",
};
```

- [ ] **Step 3: 用 REST API 驗證資料庫可讀寫**（`<databaseURL>` 換成實際值）

```bash
curl -s "<databaseURL>/soldOut.json"
# Expected: null

curl -s -X PUT -d "true" "<databaseURL>/soldOut/test.json"
# Expected: true

curl -s -X DELETE "<databaseURL>/soldOut/test.json"
# Expected: null

curl -s -X PUT -d "true" "<databaseURL>/other.json"
# Expected: {"error":"Permission denied"}（規則只開放 soldOut）
```

- [ ] **Step 4: Commit**

```bash
git add js/firebase-config.js
git commit -m "新增 Firebase 連線設定

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 菜單頁接上 Firebase 即時同步

**Files:**
- Modify: `index.html`（載入 `js/firebase-config.js`）
- Modify: `js/menu.js`（檔案末尾 `renderMenu();` 與 `export` 之間插入 Firebase 區塊，並加上 import）

**Interfaces:**
- Consumes: `FIREBASE_CONFIG`（Task 3）、`applySoldOut()`（Task 2）、`#sync-banner`（Task 1/2）
- Produces: 菜單頁完整行為——收到第一筆快照前顯示同步提示，之後即時套用賣完狀態。

- [ ] **Step 1: 修改 `index.html`**——`menu-data.js` 之後、`js/menu.js` 之前補載入設定檔（模組會用到全域 `FIREBASE_CONFIG`）：

```html
  <script src="menu-data.js"></script>
  <script src="js/firebase-config.js"></script>
  <script type="module" src="js/menu.js"></script>
```

- [ ] **Step 2: 修改 `js/menu.js`**——檔案最上方加 import：

```js
import { initializeApp } from "https://www.gstatic.com/firebasejs/12.0.0/firebase-app.js";
import { getDatabase, ref, onValue } from "https://www.gstatic.com/firebasejs/12.0.0/firebase-database.js";
```

檔案末尾把 `renderMenu();` 之後（`export` 之前）改為：

```js
renderMenu();

// ===== Firebase 即時同步 =====
// sync-banner 預設顯示；收到第一筆快照才隱藏。連線失敗時 onValue 不會觸發，
// 提示會一直留著（菜單本身仍照常顯示）——這就是規格要的「同步中」降級行為。
const app = initializeApp(FIREBASE_CONFIG);
const db = getDatabase(app);
const banner = document.getElementById("sync-banner");

onValue(
  ref(db, "soldOut"),
  (snapshot) => {
    banner.hidden = true;
    applySoldOut(snapshot.val() ?? {});
  },
  (error) => {
    banner.textContent = "賣完狀態同步失敗，顯示的供應狀態可能不是最新";
    banner.hidden = false;
    console.error("Firebase 同步錯誤：", error);
  }
);

export { renderMenu, applySoldOut };
```

- [ ] **Step 3: 本機驗證即時同步**（`<databaseURL>` 換成實際值；本機伺服器保持運行）

1. 開 `http://localhost:8000/`，頂端同步提示應在一秒內**消失**（已連上，資料為空）
2. 用 curl 模擬後台把叉燒飯設為賣完：
```bash
curl -s -X PUT -d "true" "<databaseURL>/soldOut/b01.json"
```
Expected: **不重新整理**，叉燒飯一秒內整列變灰、價格劃線、出現紅框「已賣完」印章
3. 恢復供應：
```bash
curl -s -X DELETE "<databaseURL>/soldOut/b01.json"
```
Expected: 叉燒飯一秒內恢復正常樣式
4. Console 無紅字錯誤

- [ ] **Step 4: Commit**

```bash
git add index.html js/menu.js
git commit -m "菜單頁接上 Firebase 賣完狀態即時同步

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: 後台管理頁（登入＋開關＋全部恢復）

**Files:**
- Create: `admin.html`
- Create: `css/admin.css`
- Create: `js/admin.js`

**Interfaces:**
- Consumes: `MENU_DATA`、`FIREBASE_CONFIG`、`css/base.css`
- Produces: 後台完整功能。帳密常數 `ADMIN_USER` / `ADMIN_PASS` 在 `js/admin.js` 最上方。

- [ ] **Step 1: 建立 `admin.html`**

```html
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="robots" content="noindex">
  <title>名門燒臘｜後台管理</title>
  <link rel="stylesheet" href="css/base.css">
  <link rel="stylesheet" href="css/admin.css">
</head>
<body>
  <div id="sync-banner" hidden>賣完狀態同步中，請稍候⋯</div>

  <section id="login-view" class="login-card" hidden>
    <h1>名門燒臘 後台</h1>
    <form id="login-form">
      <label>帳號
        <input id="login-user" autocomplete="username" required>
      </label>
      <label>密碼
        <input id="login-pass" type="password" autocomplete="current-password" required>
      </label>
      <p id="login-error" class="error" hidden>帳號或密碼錯誤</p>
      <button type="submit">登入</button>
    </form>
  </section>

  <section id="admin-view" hidden>
    <header class="admin-header">
      <h1>品項供應管理</h1>
      <div class="admin-actions">
        <button id="restore-all">全部恢復供應</button>
        <button id="logout" class="ghost">登出</button>
      </div>
    </header>
    <main id="admin-root"></main>
  </section>

  <script src="menu-data.js"></script>
  <script src="js/firebase-config.js"></script>
  <script type="module" src="js/admin.js"></script>
</body>
</html>
```

- [ ] **Step 2: 建立 `js/admin.js`**

```js
import { initializeApp } from "https://www.gstatic.com/firebasejs/12.0.0/firebase-app.js";
import { getDatabase, ref, onValue, set, remove } from "https://www.gstatic.com/firebasejs/12.0.0/firebase-database.js";

// ★★ 後台帳密：直接改下面兩行。 ★★
// 已知限制（需求方知情接受）：這是前端檢查，公開 repo 看得到原始碼，僅防路人誤入。
const ADMIN_USER = "admin";
const ADMIN_PASS = "mingmen2026";
const LOGIN_KEY = "mingmen-admin-login";

// ===== 登入 =====
const loginView = document.getElementById("login-view");
const adminView = document.getElementById("admin-view");
const banner = document.getElementById("sync-banner");

function setLoggedIn(loggedIn) {
  loginView.hidden = loggedIn;
  adminView.hidden = !loggedIn;
  banner.hidden = !loggedIn || bannerDismissed;
}
let bannerDismissed = false;

document.getElementById("login-form").addEventListener("submit", (e) => {
  e.preventDefault();
  const ok =
    document.getElementById("login-user").value === ADMIN_USER &&
    document.getElementById("login-pass").value === ADMIN_PASS;
  document.getElementById("login-error").hidden = ok;
  if (ok) {
    localStorage.setItem(LOGIN_KEY, "1");
    setLoggedIn(true);
  }
});

document.getElementById("logout").addEventListener("click", () => {
  localStorage.removeItem(LOGIN_KEY);
  setLoggedIn(false);
});

// ===== 品項清單 =====
const toggles = new Map(); // id -> checkbox element

function renderItems() {
  const rootEl = document.getElementById("admin-root");
  for (const cat of [MENU_DATA.bento, MENU_DATA.platter]) {
    const h2 = document.createElement("h2");
    h2.className = "admin-cat";
    h2.textContent = cat.title;
    const list = document.createElement("ul");
    list.className = "admin-list";
    for (const item of cat.items) {
      const li = document.createElement("li");
      li.innerHTML =
        `<span class="admin-name">${item.name}</span>` +
        `<label class="switch">` +
        `<input type="checkbox" data-id="${item.id}">` +
        `<span class="slider"></span>` +
        `<span class="state"></span>` +
        `</label>`;
      const input = li.querySelector("input");
      toggles.set(item.id, input);
      input.addEventListener("change", () => saveToggle(item.id, input));
      list.append(li);
    }
    rootEl.append(h2, list);
  }
}

// ===== Firebase =====
const app = initializeApp(FIREBASE_CONFIG);
const db = getDatabase(app);
const soldOutRef = ref(db, "soldOut");

// 開關狀態一律以 onValue 快照為準：寫入成功會回推、多裝置同開後台也會同步
onValue(soldOutRef, (snap) => {
  bannerDismissed = true;
  banner.hidden = true;
  const soldOut = snap.val() ?? {};
  for (const [id, input] of toggles) input.checked = soldOut[id] === true;
});

function saveToggle(id, input) {
  const target = ref(db, "soldOut/" + id);
  const action = input.checked ? set(target, true) : remove(target);
  action.catch((err) => {
    input.checked = !input.checked; // 寫入失敗：開關彈回
    alert("儲存失敗，請再試一次\n" + err.message);
  });
}

document.getElementById("restore-all").addEventListener("click", () => {
  if (confirm("確定要把全部品項恢復為「供應中」嗎？")) {
    remove(soldOutRef).catch((err) => alert("操作失敗\n" + err.message));
  }
});

// ===== 啟動 =====
renderItems();
setLoggedIn(localStorage.getItem(LOGIN_KEY) === "1");
```

- [ ] **Step 3: 建立 `css/admin.css`**

```css
/* ===== 登入卡片 ===== */
.login-card {
  max-width: 360px;
  margin: 15vh auto 0;
  background: #fff;
  border-radius: 12px;
  padding: 28px;
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
}
.login-card h1 { font-size: 20px; text-align: center; margin-bottom: 18px; }
.login-card label { display: block; margin-bottom: 12px; font-size: 14px; }
.login-card input {
  width: 100%;
  padding: 12px;
  font-size: 16px;
  border: 1px solid #ccc;
  border-radius: 8px;
  margin-top: 4px;
}
.login-card button {
  width: 100%;
  padding: 13px;
  font-size: 16px;
  background: var(--brown);
  color: #fff;
  border: 0;
  border-radius: 8px;
}
.error { color: var(--red); font-size: 14px; margin-bottom: 10px; }

/* ===== 管理畫面 ===== */
#admin-view { max-width: 1000px; margin: 0 auto; }
.admin-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 12px;
  padding: 8px 0 16px;
}
.admin-header h1 { font-size: 22px; }
.admin-actions { display: flex; gap: 10px; }
#restore-all {
  background: var(--red);
  color: #fff;
  border: 0;
  border-radius: 8px;
  padding: 12px 16px;
  font-size: 15px;
}
.ghost {
  background: transparent;
  border: 1px solid var(--brown);
  color: var(--brown);
  border-radius: 8px;
  padding: 12px 16px;
  font-size: 15px;
}

.admin-cat { font-size: 17px; color: var(--brown); margin: 18px 0 10px; letter-spacing: 4px; }
.admin-list {
  list-style: none;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px 24px;
}
.admin-list li {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: #fff;
  border-radius: 10px;
  padding: 8px 14px;
  min-height: 56px; /* 手機好按、防誤觸 */
}
.admin-name { font-size: 16px; }

/* ===== 開關 ===== */
.switch { display: flex; align-items: center; gap: 10px; }
.switch input { position: absolute; opacity: 0; }
.slider {
  width: 52px;
  height: 30px;
  border-radius: 999px;
  background: #9dbfa3; /* 綠＝供應中 */
  position: relative;
  transition: background 0.2s;
  flex-shrink: 0;
}
.slider::after {
  content: "";
  position: absolute;
  top: 3px;
  left: 3px;
  width: 24px;
  height: 24px;
  border-radius: 50%;
  background: #fff;
  transition: left 0.2s;
}
.switch input:checked + .slider { background: var(--red); }
.switch input:checked + .slider::after { left: 25px; }
.state { width: 3.5em; font-size: 14px; }
.state::after { content: "供應中"; color: #3a7d44; }
.switch input:checked ~ .state::after { content: "已賣完"; color: var(--red); }

/* ===== RWD ===== */
@media (max-width: 640px) {
  .admin-list { grid-template-columns: 1fr; }
  .login-card { margin-top: 8vh; }
  .admin-header h1 { font-size: 19px; }
}
```

- [ ] **Step 4: 本機驗證後台全流程**（本機伺服器保持運行）

開 `http://localhost:8000/admin.html`，依序檢查：
1. 顯示置中登入卡片；輸入錯誤帳密 → 出現紅字「帳號或密碼錯誤」
2. 輸入 `admin` / `mingmen2026` → 進入管理畫面，便當/拼盤共 30 列開關，全部「供應中」
3. 把「叉燒飯」切為已賣完 → 開關變紅、文字變「已賣完」
4. **另開一個分頁**到 `http://localhost:8000/` → 叉燒飯呈賣完樣式；在後台切回供應中 → 菜單分頁一秒內恢復（不重整）
5. 按「全部恢復供應」→ 出現確認框 → 確定後所有開關回到「供應中」
6. 重新整理 admin.html → 免登入直接進管理畫面；按「登出」→ 回到登入卡片
7. DevTools 切手機寬度（375px）→ 清單變單欄、開關夠大好按
8. Console 無紅字錯誤

Expected: 以上全部符合。

- [ ] **Step 5: Commit**

```bash
git add admin.html css/admin.css js/admin.js
git commit -m "新增後台管理頁：登入、賣完開關、全部恢復供應

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: README、部署 GitHub Pages 與真機驗證

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: 前五個任務的全部產出
- Produces: 上線的網站（`https://lemel917.github.io/FamousRoastMeat/`）

- [ ] **Step 1: 建立 `README.md`**

```markdown
# 名門燒臘電子菜單

兩頁式靜態網站，部署於 GitHub Pages，賣完狀態透過 Firebase Realtime Database 即時同步。

- **菜單頁**：`index.html` — 給客人看，賣完品項自動變灰並蓋「已賣完」章
- **後台頁**：`admin.html` — 老闆登入後切換各品項「供應中／已賣完」

## 常見維護

| 要做什麼 | 改哪裡 |
|---|---|
| 改品名 / 價格 / 標籤 | `menu-data.js`（改完 push 即生效，不要動 id） |
| 改後台帳密 | `js/admin.js` 最上方 `ADMIN_USER` / `ADMIN_PASS` |
| 換 Firebase 專案 | `js/firebase-config.js` |

## 本機開發

    python -m http.server 8000

開 http://localhost:8000/ 與 http://localhost:8000/admin.html （ES Modules 不能用 file:// 直接開檔）。

## 已知限制

後台帳密為前端檢查、資料庫 `soldOut` 開放寫入——僅防路人誤入，非資安防護（詳見 `docs/superpowers/specs/`）。
```

- [ ] **Step 2: Commit 並請使用者 push**

```bash
git add README.md docs/superpowers/plans/2026-07-18-電子菜單.md
git commit -m "新增 README 與實作計畫

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

push 由使用者自行執行（或經使用者同意後執行 `git push origin main`）。

- [ ] **Step 3: 開啟 GitHub Pages**（使用者操作）

> GitHub repo → Settings → Pages → Source 選「Deploy from a branch」→ Branch 選 `main` / `/ (root)` → Save。等 1–2 分鐘後網站網址為 `https://lemel917.github.io/FamousRoastMeat/`。

- [ ] **Step 4: 線上驗證**

1. 手機開 `https://lemel917.github.io/FamousRoastMeat/` → 菜單單欄顯示、同步提示一秒內消失
2. 另一台裝置（或電腦）開 `.../FamousRoastMeat/admin.html` → 登入 → 切「三寶飯」為已賣完
3. Expected: 手機上的菜單**不重整**，三寶飯一秒內變灰蓋章；「全部恢復供應」後恢復
4. 提醒使用者：正式使用前把 `js/admin.js` 的預設密碼改掉，並把菜單頁網址做成 QR code

Expected: 跨裝置即時同步成功，專案完成。
